<h2>固定費科目の詳細</h2>
<%= link_to "編集",edit_fixedcost_path(@fixedcost.id) %>
<%= link_to "固定費科目一覧へ戻る", fixedcosts_path,method: :get %>

<table>
  <tr>
    <th>名称</th>
    <td><%= @fixedcost.subject %></td>
  </tr>
  <tr>
    <th>説明</th>
    <td><%= @fixedcost.remark %></td>
  </tr>
</table>