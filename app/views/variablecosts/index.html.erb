<%= link_to "変動費科目の新規登録", new_variablecost_path %>
<% if @variablecosts.present? %>
<table>
  <thead>
    <tr>
      <th>科目名</th>
      <th>備考</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @variablecosts.each do |variablecost| %>
      <tr>
        <td><%= variablecost.subject %></td>
        <td><%= variablecost.remark %></td>
        <td>
          <%#= link_to "編集",edit_variablecost_path(@variablecost.id) %>
          <%#= link_to "削除", variablecost_path(@variablecost.id), method: :delete,"data-confirm"=>"本当に削除しますか?" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
 
<% else %>
  <p>登録されている収入科目がありません。</p>
<% end %>